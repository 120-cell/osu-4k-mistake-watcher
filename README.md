## Features
https://github.com/user-attachments/assets/53a44f1d-32c4-45d1-a098-932e116f0208

## Requirements
python 3.10 or above, as well as several python modules. To install them, run:
```
pip install keyboard
pip install mouse
pip install pyyaml
pip install apscheduler
pip install playsound==1.2.2
```
## Running the App
To start the app, run main.py

On linux, sudo is required to read keystrokes while out of focus.
